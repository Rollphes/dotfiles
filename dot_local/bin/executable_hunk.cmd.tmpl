@echo off
setlocal

set "HUNK_TEMP={{ .chezmoi.homeDir }}\.cache\hunk\tmp"
if not exist "%HUNK_TEMP%" mkdir "%HUNK_TEMP%" || exit /b 1
set "BUN_TMPDIR=%HUNK_TEMP%"
set "TEMP=%HUNK_TEMP%"
set "TMP=%HUNK_TEMP%"

"%~dp0..\share\mise\shims\hunk.exe" %*
exit /b %ERRORLEVEL%
